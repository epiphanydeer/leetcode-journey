# 02. Two Pointers / 10_grouped_iteration

## 解题记录

- 题号与链接：
- 核心思路：
- 不变量 / 状态定义：
- 时间复杂度：
- 空间复杂度：
- 边界用例：

## Python 起手模板

```python
def solve(nums):
    left, right = 0, len(nums) - 1
    while left < right:
        # compare/process nums[left] and nums[right]
        # move one or both pointers according to the invariant
        pass
```