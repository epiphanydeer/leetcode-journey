# 08. Dynamic Programming / 12_probability_expectation

## 解题记录

- 题号与链接：
- 核心思路：
- 不变量 / 状态定义：
- 时间复杂度：
- 空间复杂度：
- 边界用例：

## Python 起手模板

```python
def solve(nums):
    # Define dp[i] before writing this loop.
    dp = [0] * (len(nums) + 1)
    for i in range(1, len(dp)):
        # dp[i] = transition from earlier states
        pass
    return dp[-1]
```